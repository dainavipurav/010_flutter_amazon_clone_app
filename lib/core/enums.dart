enum QuantityAction {
  increase,
  decrease,
  add,
  remove,
}

enum AddressType {
  home,
  work,
  other,
}

enum PaymentType {
  card,
  internetBanking,
  upi,
  cod,
}

enum PaymentStatus {
  success,
  fail,
  wallet,
}

enum Gender {
  male,
  female,
  other,
}

enum ImagePickerType {
  files,
  camera,
}
